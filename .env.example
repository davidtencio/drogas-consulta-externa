# Activa una sesión local con datos ficticios y sin Firebase Auth/Firestore.
# No definir esta variable en producción.
NEXT_PUBLIC_DEMO_MODE=0

# Identifica visualmente un despliegue autenticado de piloto controlado.
NEXT_PUBLIC_PILOT_MODE=0
